<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="dev.kzone.portfolio.userapi.mapper.UserMapper">

    <resultMap id="userResultMap" type="dev.kzone.portfolio.userapi.domain.User">
        <id property="id" column="id"/>
        <result property="name" column="name"/>
        <result property="email" column="email"/>
        <result property="createdAt" column="created_at"/>
    </resultMap>

    <select id="findById" resultMap="userResultMap">
        SELECT id, name, email, created_at
        FROM users
        WHERE id = #{id}
    </select>

    <select id="search" resultMap="userResultMap">
        SELECT id, name, email, created_at
        FROM users
        <where>
            <if test="keyword != null">
                AND (LOWER(name) LIKE CONCAT('%', #{keyword}, '%')
                     OR LOWER(email) LIKE CONCAT('%', #{keyword}, '%'))
            </if>
            <if test="emailDomain != null">
                AND LOWER(email) LIKE CONCAT('%@', #{emailDomain})
            </if>
        </where>
        ORDER BY id
    </select>

    <insert id="insert" parameterType="dev.kzone.portfolio.userapi.domain.User"
            useGeneratedKeys="true" keyProperty="id">
        INSERT INTO users (name, email)
        VALUES (#{name}, #{email})
    </insert>

    <update id="update" parameterType="dev.kzone.portfolio.userapi.domain.User">
        UPDATE users
        SET name = #{name},
            email = #{email}
        WHERE id = #{id}
    </update>

    <delete id="deleteById">
        DELETE FROM users WHERE id = #{id}
    </delete>
</mapper>
